//! OpenAI: speaks the OpenAI-compatible protocol. Its model roster lives
//! under [`models`]; parameter support is passthrough — OpenAI itself
//! rejects what it doesn't accept.

pub(crate) mod models;
